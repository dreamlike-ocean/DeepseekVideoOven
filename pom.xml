<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.github.dreamlike</groupId>
    <artifactId>DeepseekVideoOven</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <properties>
        <maven.compiler.release>25</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <native.res.dir>${project.basedir}/src/main/resources/native</native.res.dir>
        <native.build.dir>${project.build.directory}/native-build</native.build.dir>
        <jackson.version>2.18.2</jackson.version>
        <main.class>io.github.dreamlike.deepseekvideooven.App</main.class>
        <skip.native.build>false</skip.native.build>
        <video.oven.cuda>AUTO</video.oven.cuda>
        <video.oven.metal>AUTO</video.oven.metal>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>25</release>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-clean-plugin</artifactId>
                <version>3.4.0</version>
                <executions>
                    <execution>
                        <id>clean-native-libs</id>
                        <phase>clean</phase>
                        <goals><goal>clean</goal></goals>
                        <configuration>
                            <skip>${skip.native.build}</skip>
                            <filesets>
                                <fileset>
                                    <directory>${native.res.dir}</directory>
                                    <includes>
                                        <include>*.so*</include>
                                        <include>*.dylib*</include>
                                        <include>*.dll</include>
                                        <include>libs.txt</include>
                                    </includes>
                                </fileset>
                            </filesets>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.6.0</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals><goal>shade</goal></goals>
                        <configuration>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>${main.class}</mainClass>
                                </transformer>
                            </transformers>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.5.0</version>
                <executions>
                    <execution>
                        <id>whisper-cmake-clean</id>
                        <phase>clean</phase>
                        <goals><goal>exec</goal></goals>
                        <configuration>
                            <executable>sh</executable>
                            <arguments>
                                <argument>-c</argument>
                                <argument>test -d ${native.build.dir} &amp;&amp; cmake --build ${native.build.dir} --target clean || true</argument>
                            </arguments>
                            <skip>${skip.native.build}</skip>
                        </configuration>
                    </execution>
                    <execution>
                        <id>whisper-cmake-configure</id>
                        <phase>generate-sources</phase>
                        <goals><goal>exec</goal></goals>
                        <configuration>
                            <executable>cmake</executable>
                            <workingDirectory>${project.basedir}</workingDirectory>
                            <arguments>
                                <argument>-S</argument>
                                <argument>native</argument>
                                <argument>-B</argument>
                                <argument>${native.build.dir}</argument>
                                <argument>-DCMAKE_BUILD_TYPE=Release</argument>
                                <argument>-DVIDEO_OVEN_NATIVE_DIR=${native.res.dir}</argument>
                                <argument>-DVIDEO_OVEN_CUDA=${video.oven.cuda}</argument>
                                <argument>-DVIDEO_OVEN_METAL=${video.oven.metal}</argument>
                            </arguments>
                            <skip>${skip.native.build}</skip>
                        </configuration>
                    </execution>
                    <execution>
                        <id>whisper-cmake-build</id>
                        <phase>generate-sources</phase>
                        <goals><goal>exec</goal></goals>
                        <configuration>
                            <executable>cmake</executable>
                            <workingDirectory>${project.basedir}</workingDirectory>
                            <arguments>
                                <argument>--build</argument><argument>${native.build.dir}</argument>
                                <argument>--config</argument><argument>Release</argument>
                                <argument>--target</argument><argument>video_oven_native_bundle</argument>
                                <argument>-j</argument>
                            </arguments>
                            <skip>${skip.native.build}</skip>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

        </plugins>
    </build>

    <profiles>
        <profile>
            <id>native</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.graalvm.buildtools</groupId>
                        <artifactId>native-maven-plugin</artifactId>
                        <version>0.10.5</version>
                        <extensions>true</extensions>
                        <configuration>
                            <imageName>video-oven</imageName>
                            <mainClass>${main.class}</mainClass>
                            <buildArgs>
                                <arg>-H:+ForeignAPISupport</arg>
                                <arg>--enable-native-access=ALL-UNNAMED</arg>
                            </buildArgs>
                        </configuration>
                        <executions>
                            <execution>
                                <id>build-native</id>
                                <goals><goal>compile-no-fork</goal></goals>
                                <phase>package</phase>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <profile>
            <id>dev</id>
            <properties>
                <skip.native.build>true</skip.native.build>
            </properties>
        </profile>
    </profiles>
</project>
